@model IEnumerable<Ints.DesafioInts.Application.ViewModels.PorteEmpresaViewModel>

@{
    ViewBag.Title = "Listar Porte Empresa";
}

<h2>Porte Empresas</h2>
<hr /><br />

<p>
    @Html.ActionLink("Cadastrar Porte Empresa", "Create", "", new { @class = "btn btn-default" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>Opções</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.ActionLink("Atualizar", "Edit", new { id=item.PorteEmpresaId }) |
            @Html.ActionLink("Visualizar", "Details", new { id=item.PorteEmpresaId }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.PorteEmpresaId })
        </td>
    </tr>
}

</table>
