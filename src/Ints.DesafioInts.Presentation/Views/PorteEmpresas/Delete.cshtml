@model Ints.DesafioInts.Application.ViewModels.PorteEmpresaViewModel

@{
    ViewBag.Title = "Delete";
}

<h3>Excluir Porte Empresa</h3>
<hr />

<h4>Tem certeza que deseja deletar o porte de empresa abaixo?</h4>
<div class="form-group">
    <div class="row">
        <div class="col-md-12">
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.Descricao)
                </dt>
                <dd>
                    @Html.DisplayFor(model => model.Descricao)
                </dd>
            </dl>
        </div>
    </div>
</div>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="btn-group">
        @Html.ActionLink("Voltar", "Index", "", new { @class = "btn btn-default" })
        <input type="submit" value="Excluir" class="btn btn-danger" />
    </div>
}
